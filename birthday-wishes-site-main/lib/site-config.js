// This deploys to Vercel, which serves from the root domain, so basePath
// is always empty. (GitHub Pages support is paused — see README — since
// real password auth requires a server, which static export can't provide.)
const basePath = ''

module.exports = { basePath }
